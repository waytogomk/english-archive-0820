# ENGLISH ARCHIVE — Design System & UI Direction

## 1. 문서 목적

이 문서는 ENGLISH ARCHIVE의 학생용·관리자용 화면에 공통 적용할 시각 디자인 기준이다.

- 기능과 데이터 요구사항은 [`PRD.md`](./PRD.md)를 따른다.
- 색상, 타이포그래피, 카드, 이미지, 반응형 화면 등 시각적 판단은 이 문서를 따른다.
- `PRD.md`의 기존 디자인 방향과 충돌할 경우 **이 문서가 최신 기준**이다.
- 디자인을 수정해도 등록된 학교, 문제, 시험지, 책, 학습 기록은 삭제하거나 초기화하지 않는다.

## 2. 레퍼런스

주요 레퍼런스: [Renaissance 공식 사이트](https://www.renaissance.com/)

Renaissance에서 참고할 부분:

- 넓은 여백과 큰 제목을 중심으로 한 차분한 에듀테크 편집 레이아웃
- 짙은 네이비, 선명한 포인트 컬러, 크림·화이트를 조합한 성숙한 팔레트
- 긴 설명보다 짧은 메시지, 분명한 CTA, 정보 단위별 카드로 구성하는 방식
- 서비스 영역을 한 화면에 모두 늘어놓지 않고 큰 섹션과 명확한 위계로 나누는 방식
- 학습 데이터와 콘텐츠 이미지를 장식이 아닌 실제 정보로 사용하는 방식
- 둥근 모서리를 사용하되 검은 외곽선과 장난감 같은 3D 효과를 피하는 방식

그대로 복사하지 않을 부분:

- 로고, 문구, 화면 배치, 아이콘, 이미지 및 고유 브랜드 요소
- Renaissance 로고, 고유 서체, 문구, 서비스의 기능 구조 및 콘텐츠 명칭
- 저작권이 있는 그래픽 자산

ENGLISH ARCHIVE는 Renaissance의 **성숙한 교육 브랜드 톤, 넓은 여백, 정돈된 학습 정보 구조**만 참고한다. 기존 English Archive의 파랑·초록·주황·보라 팔레트는 유지하며 레퍼런스의 색을 그대로 적용하지 않는다.

## 3. 디자인 콘셉트

### 핵심 키워드

`Academic · Focused · Confident · Trustworthy · Modern`

### 목표 인상

- 중·고등학생에게 자연스럽고 유치하지 않은 학습 서비스
- 열심히 공부하는 분위기는 있지만 학원 관리 프로그램처럼 딱딱하지 않은 화면
- 밝고 선명하되, 짙은 네이비와 아카데믹 블루를 기준색으로 잡아 장난감처럼 산만하지 않은 화면
- 교재, 시험, 책 표지와 학습 상태가 가장 먼저 보이는 화면

### 피해야 할 인상

- 두껍고 검은 테두리로 둘러싼 만화식 카드
- 카드 아래를 가득 채우는 큰 학교 건물·책·연필 SVG
- 초등 영어 교재처럼 보이는 캐릭터와 손그림
- 사람 또는 대학생 사진이 중심이 되는 히어로 이미지
- 텍스트와 장식 이미지가 겹치는 화면
- 과도한 그림자, 3D 버튼, 네온색, 복잡한 그라데이션
- `S`, `B`, `文`, `Aa` 같은 문자 한 글자 아이콘

## 4. 브랜드 메시지

홈 히어로의 영문 메시지는 아래 문구를 정확히 사용한다.

> **Build your academic confidence!**

메인 한글 메시지:

> 필요할 때 꺼내 푸는 나만의 영어 보물창고, English Archive!

- 첫 줄 `필요할 때 꺼내 푸는`은 기본 본문색을 사용한다.
- `나만의 영어 보물창고, English Archive!`는 짙은 네이비 `#1D3159`로 강조한다.
- `오늘 집중할 학습을 선택하세요` 영역과 주요 CTA는 선명한 블루 `#1769D2`를 사용한다.

보조 문구:

> 학교 시험부터 문법, 원서리딩까지.  
> 풀고, 확인하고, 내 실력을 차곡차곡 쌓아보세요.

브랜드명은 항상 **ENGLISH ARCHIVE**로 표기하며 `MK`는 사용하지 않는다.

## 5. 정보 구조와 메뉴 표기

학생 홈의 큰 메뉴는 2개만 노출한다.

1. **중고등 내신**
2. **원서리딩**

하위 구조:

```text
중고등 내신
├─ 학교별 내신
│  └─ 학교 선택
│     ├─ 기출 모의고사
│     └─ 학교별 어휘학습
└─ 문법학습

원서리딩
└─ 책 제목
   └─ Chapter/범위별 Reading Quiz
```

홈에서는 세화여중 등 특정 학교 이름을 노출하지 않는다.

## 6. 색상 시스템

전체 화면은 기존 English Archive의 파랑을 중심으로 구성한다. 화이트·옅은 블루 배경으로 여백을 확보하고 초록·주황·보라는 학습 종류와 상태 구분에 제한적으로 사용한다. Renaissance에서는 색이 아니라 여백과 정보 위계만 참고한다.

| 토큰 | 값 | 사용처 |
|---|---:|---|
| `--background` | `#F7FAFD` | 전체 배경 |
| `--surface` | `#FFFFFF` | 카드, 퀴즈, 관리자 패널 |
| `--surface-soft` | `#E8F3FC` | 보조 영역, 학습 현황 |
| `--ink` | `#151A23` | 제목, 핵심 본문 |
| `--muted` | `#687386` | 설명, 메타 정보 |
| `--brand` | `#1769D2` | 주요 CTA, 링크, 큰 학습 영역 |
| `--brand-bright` | `#3B86E8` | 진행 상태와 강조 |
| `--brand-soft` | `#EAF3FF` | 선택 상태, 약한 블루 배경 |
| `--green` | `#60BB46` | 완료, 어휘학습 포인트 |
| `--orange` | `#FF9933` | 보상과 주의 포인트 |
| `--purple` | `#7C55C6` | 문법과 원서리딩 포인트 |
| `--line` | `#DDE5EE` | 1px 경계선 |
| `--success` | `#2F7D58` | 정답, 완료 |
| `--danger` | `#B84052` | 오답, 오류 |

규칙:

- 검정색 `2px~3px` 외곽선은 사용하지 않는다.
- 카드 구분이 필요할 때는 `1px solid var(--line)`을 사용한다.
- 한 카드 안의 강한 포인트 색상은 1개, 한 섹션 안에서는 최대 2개로 제한한다.
- 색만으로 상태를 알리지 않고 아이콘과 텍스트를 함께 표시한다.

## 7. 타이포그래피

### 적용 글꼴

- 한글·영문 공통: `Pretendard Variable`
- fallback: `Pretendard`, `Noto Sans KR`, system sans-serif
- npm의 `pretendard` 패키지를 사용해 브라우저·운영체제와 관계없이 같은 글꼴을 제공한다.
- Renaissance의 본문 `Proxima Nova` + 제목 `Expo Serif Pro` 조합을 참고한다.
- Adobe Fonts의 고유 서체를 복제하거나 hotlink하지 않고, OFL 라이선스의 `Newsreader Variable`을 영문 브랜드 `ENGLISH ARCHIVE`와 히어로의 `English Archive!`에 제한적으로 사용한다.
- 한글 제목·본문·퀴즈는 현대적인 `Pretendard Variable`로 유지해 오래된 교재처럼 보이지 않게 한다.
- 손글씨체와 장식용 serif는 사용하지 않는다.

### 크기 체계

| 역할 | Desktop | Mobile | 굵기 |
|---|---:|---:|---:|
| Hero title | `56–72px` | `40–48px` | 800–900 |
| Page title | `44–56px` | `34–42px` | 800–900 |
| Section title | `28–36px` | `24–30px` | 750–850 |
| Card title | `22–28px` | `20–24px` | 750–850 |
| Body | `16–18px` | `15–16px` | 400–600 |
| Meta/label | `11–13px` | `11–12px` | 700–850 |

규칙:

- 한글 제목의 자간은 `-0.03em~-0.05em` 범위로 사용한다.
- 영문 eyebrow는 모두 대문자로 쓰지 않아도 되며, 읽기 쉬운 문구는 일반 문장형을 우선한다.
- 본문 줄간격은 `1.6~1.75`를 유지한다.

## 8. 레이아웃

- 최대 콘텐츠 폭: `1180–1200px`
- Desktop 좌우 여백: `28–40px`
- Mobile 좌우 여백: `18–20px`
- 섹션 간격: Desktop `72–96px`, Mobile `52–68px`
- 카드 간격: Desktop `18–24px`, Mobile `12–16px`
- 기본 모서리: 카드 `20–24px`, 작은 요소 `10–14px`, pill `999px`

텍스트와 이미지는 서로 겹치지 않아야 한다. 장식 이미지는 콘텐츠 뒤에 깔지 않고, 별도 열 또는 독립된 배경 레이어에 둔다.

## 9. 홈 화면

### 9.1 헤더

- 왼쪽에 `ENGLISH ARCHIVE`를 동일한 크기의 두 단어로 배치한다.
- 불필요한 심볼 로고는 사용하지 않는다.
- 학생 메뉴와 관리자 메뉴를 동시에 노출하지 않는다.
- `TREAT FRIDGE`는 다른 메뉴와 구분되는 부드러운 포인트 색을 사용한다.
- 모바일에서는 메뉴를 가로 스크롤 pill 또는 간결한 메뉴 버튼으로 제공한다.

### 9.2 히어로

- 현재 사용 중인 잔잔한 자연광 배경 이미지를 크림 오버레이와 함께 유지한다.
- 사람 이미지는 사용하지 않는다.
- 고양이·동물은 화면의 주인공이 아니라 자연스러운 작은 배경 요소로만 허용한다.
- 텍스트 가독성을 위해 이미지 위에 따뜻한 반투명 오버레이를 적용한다.
- 오른쪽 학습 현황 패널은 흰색 반투명 surface, 얇은 중성 테두리, 네이비 텍스트를 사용한다.
- `EXAM PREP`, `D-DAY` 같은 장식성 정보는 넣지 않는다.

### 9.3 큰 학습 메뉴 카드

기존의 두꺼운 검정 테두리와 큰 하단 SVG는 제거한다.

Desktop:

- 2열 카드
- 최소 높이 `260–320px`
- `1px` 중성 경계선 또는 경계선 없이 아주 약한 그림자
- 제목, 설명, 진행 상태, CTA 순서
- 오른쪽 위에 작은 아이콘 또는 절제된 추상 도형 1개만 사용
- hover 시 `translateY(-3px)`와 부드러운 그림자만 적용

Mobile:

- 1열 카드
- 최소 높이 `220px` 이하를 권장
- CTA는 카드 아래에 가려지지 않게 배치
- 장식은 텍스트 영역의 25%를 넘지 않는다.

카드별 톤:

- 중고등 내신: white + blue/green accent
- 원서리딩: white + purple accent

## 10. 하위 메뉴와 목록 카드

### 학교·문법·학습 메뉴

- 카드 전체가 클릭 가능해야 한다.
- 큰 그림 대신 작은 라인 아이콘, 번호, 상태 pill을 사용한다.
- 카드당 핵심 정보는 제목, 한 줄 설명, 상태, 이동 표시로 제한한다.
- `border: 1px solid var(--line)`과 매우 약한 그림자를 사용한다.
- 완료·학습 중·재학습 필요 상태는 항상 텍스트로 표시한다.

### 최근 학습

- 제목, 범위, 진행률, 마지막 학습일을 한눈에 보여준다.
- 큰 장식 이미지 없이 진행률과 상태가 중심이 된다.

## 11. 원서리딩과 책 표지

원서리딩 책 목록에는 임의로 만든 책 그림이 아니라 **실제 책 표지**를 사용한다.

초기 적용 대상:

- `Gangsta Granny` — David Walliams
- `Wonder` — R. J. Palacio

이미지 적용 절차:

1. Google 이미지 검색으로 정확한 제목·저자·판본을 확인한다.
2. 출판사 공식 페이지를 우선하고, 없으면 신뢰할 수 있는 서점·도서관 소스를 사용한다.
3. 검색 결과 이미지를 외부 URL로 직접 hotlink하지 않는다.
4. 사용 권한과 출처를 확인한 이미지를 프로젝트 또는 비공개 Storage에 저장한다.
5. 표지의 비율을 바꾸거나 텍스트 위에 덮어쓰지 않는다.
6. `alt`에는 `Gangsta Granny 책 표지`처럼 제목을 포함한다.

책 카드 규격:

- 표지 비율: 원본 비율 유지, 기본 표시 영역은 약 `2:3`
- 표지는 `object-fit: cover`가 아니라 가능하면 `contain`을 사용해 잘림을 막는다.
- 카드에는 표지, 제목, 저자, Chapter/범위, 문제 수, 진행 상태, 시작 버튼을 표시한다.
- 표지에 강한 인위적 테두리나 검은 offset shadow를 넣지 않는다.
- 아직 표지가 없으면 제목 첫 글자가 아닌 중성적인 책 placeholder 이미지를 사용한다.

## 12. 퀴즈 화면

- 퀴즈에서는 장식보다 문제 가독성을 우선한다.
- 질문 카드의 최대 폭은 `720–780px`로 유지한다.
- 보기 버튼은 최소 높이 `52px`, 터치 영역은 최소 `44px`다.
- 선택, 정답, 오답은 색상 + 아이콘 + 텍스트로 표현한다.
- 어휘·문법·원서리딩은 답 확인 후 카드 아래에 즉시 피드백을 펼친다.
- 기출 모의고사는 최종 제출 전 정답·오답 색을 표시하지 않는다.
- 긴 25~50문제 학습에서는 상단 진행률과 문제 번호 이동 기능을 제공한다.

## 13. Teacher's Treat Fridge

- 명칭은 영어 `Teacher's Treat Fridge`를 사용한다.
- 학습 메뉴와 구분되는 orange + soft blue 영역을 사용한다.
- 냉장고 이미지는 한 번만, 작고 자연스럽게 배치한다.
- 간식 20개는 같은 스타일의 오리지널 카와이 이미지 세트로 통일한다.
- `다음 보상: 망고`처럼 미리 정해진 보상은 표시하지 않는다.
- 사용 가능한 선택권과 학생이 직접 고르는 행동이 중심이 된다.

## 14. 관리자 화면

- 학생용과 같은 색상·글꼴을 사용하되 장식 이미지는 최소화한다.
- 관리자 카드에는 자료 수, 처리 상태, 마지막 수정일, 핵심 행동을 표시한다.
- 업로드, 분석, 검수, 배포 상태를 stepper 또는 상태 pill로 명확히 구분한다.
- 학생 화면에 공개되는 결과를 미리 볼 수 있는 `학생 화면 미리보기`를 제공한다.
- 위험한 삭제보다 보관과 새 버전 생성을 기본 행동으로 둔다.
- 입력 폼과 표는 흰색 surface, `1px` 경계선, 명확한 label을 사용한다.

## 15. 이미지와 아이콘 원칙

- 큰 메뉴 카드의 장식용 SVG 학교·책 그림은 사용하지 않는다.
- 실제 책은 실제 표지 이미지를 사용한다.
- 배경 이미지는 자연광, 종이, 책상, 창가처럼 잔잔한 장면을 사용한다.
- 사람 중심 사진은 사용하지 않는다.
- 동물은 배경의 작은 포인트로만 사용할 수 있다.
- 아이콘은 한 가지 라이브러리의 동일한 stroke 스타일로 통일한다.
- 래스터 이미지는 WebP/AVIF로 최적화하고 Next.js `Image`를 사용한다.
- 외부 이미지의 출처와 사용 가능 여부를 기록한다.

## 16. 그림자와 인터랙션

권장 그림자:

```css
--shadow-sm: 0 4px 16px rgba(45, 38, 52, 0.06);
--shadow-md: 0 12px 32px rgba(45, 38, 52, 0.09);
```

- 버튼 hover 이동은 최대 `2px`다.
- 카드 hover 이동은 최대 `3px`다.
- 검은색 offset shadow와 튀는 bounce 효과는 사용하지 않는다.
- transition은 `150–220ms` 범위로 짧게 유지한다.
- `prefers-reduced-motion`에서 움직임을 제거한다.

## 17. 접근성과 반응형

- 최소 지원 폭은 `360px`다.
- 360px에서 가로 스크롤, 잘린 제목, 겹친 이미지가 없어야 한다.
- 일반 본문 대비는 WCAG AA를 목표로 한다.
- 포커스 상태는 `2px` outline으로 명확히 표시한다.
- hover에만 의존하는 기능을 만들지 않는다.
- 이미지에는 의미 있는 대체 텍스트를 제공한다.
- 배경 이미지는 스크린리더가 읽지 않도록 장식으로 처리한다.

## 18. 컴포넌트 기준

공통 컴포넌트:

- `SiteHeader`
- `Hero`
- `LearningMenuCard`
- `StatusPill`
- `ProgressBar`
- `BookCard`
- `QuizCard`
- `FeedbackPanel`
- `TreatCard`
- `AdminStatusCard`
- `UploadStepper`

각 컴포넌트는 페이지마다 임의의 테두리·색상·radius를 새로 만들지 않고 디자인 토큰을 사용한다.

## 19. 적용 우선순위

1. `Pretendard Variable`을 전체 화면에 안정적으로 로드
2. 홈 메시지를 `필요할 때 꺼내 푸는 나만의 영어 보물창고, English Archive!`로 수정
3. 기존 파랑·초록·주황·보라 디자인 토큰을 학생·관리자 화면에 공통 적용
4. 큰 메뉴 카드와 하위 메뉴에서 검은 테두리·대형 장식 SVG 제거
5. 원서리딩에 실제 책 표지 적용
6. 최근 학습, 냉장고, 퀴즈 화면의 상태색과 CTA 통일
7. 360px, 768px, 1440px 화면 검수

## 20. 완료 체크리스트

- [ ] `Build your academic confidence!` 문구가 정확히 보인다.
- [ ] `필요할 때 꺼내 푸는 나만의 영어 보물창고, English Archive!` 문구가 정확히 보인다.
- [ ] 모든 페이지에서 `Pretendard Variable`이 적용된다.
- [ ] 홈의 큰 메뉴는 중고등 내신과 원서리딩 2개다.
- [ ] 큰 메뉴 카드에 두꺼운 검정 테두리가 없다.
- [ ] 카드 아래의 큰 학교·책 SVG가 없다.
- [ ] 텍스트와 이미지가 겹치지 않는다.
- [ ] Gangsta Granny 등 등록된 책에 실제 표지가 보인다.
- [ ] 책 표지의 출처와 사용 권한을 확인했다.
- [ ] 학생용과 관리자용 화면이 같은 디자인 토큰을 사용한다.
- [ ] 상태는 색상뿐 아니라 텍스트로도 표시된다.
- [ ] 360px 모바일에서 가로 스크롤이 없다.
- [ ] 관리자 수정·배포 과정에서 기존 자료와 학습 기록이 유지된다.
