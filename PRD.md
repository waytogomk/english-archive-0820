# ENGLISH ARCHIVE — Product Requirements Document

## 1. 문서 정보

- 문서 버전: v1.0
- 작성일: 2026-08-20
- 제품명: **ENGLISH ARCHIVE**
- 제품 유형: 모바일 우선 반응형 영어학습 웹사이트
- 배포 환경: Vercel
- 주요 사용자: 영어 교사, 중·고등학생
- 기본 언어: 한국어 UI + 영어 학습 콘텐츠

## 2. 제품 한 줄 정의

> 교사가 학교 시험 자료, 문법 자료, 챕터북 Reading Quiz를 등록하고 학생이 모바일에서 문제를 풀며 진도·오답·보상을 관리하는 영어학습 웹사이트다.

## 3. 배경과 해결할 문제

교사는 학교와 시험 시기마다 달라지는 단어 목록, 문법 프린트, 기출 시험지, 교과서 시험범위 및 원서 퀴즈를 관리해야 한다. 자료가 PDF, Word, Excel 등 여러 파일로 흩어져 있어 매번 학생에게 전달하고 채점하기 어렵다.

학생은 어떤 자료를 완료했는지 알기 어렵고, 틀린 문법 문제를 다시 학습하거나 이전 기록을 확인하기 어렵다. ENGLISH ARCHIVE는 자료 등록, 문제 생성·검수·배포, 모바일 풀이, 채점, 진도, 오답 및 보상을 하나의 서비스로 통합한다.

## 4. 제품 목표

- 교사가 학교, 자료, 문법 항목, 책을 코드 수정 없이 계속 추가한다.
- 업로드 콘텐츠를 덮어쓰지 않고 버전별로 안전하게 보존한다.
- 학생이 휴대전화에서 3번 이내 선택으로 원하는 학습을 시작한다.
- 문제 풀이, 점수, 진도, 오답, 보상 기록을 계정별로 영구 저장한다.
- 생성과 배포를 분리하여 관리자 검수 전 문제는 학생에게 노출하지 않는다.
- Vercel에 배포하여 설치 없이 웹주소로 접속한다.

### 비목표

- MVP에서 영상 강의, 실시간 화상수업, 학생 간 SNS는 제공하지 않는다.
- 학교 성적표나 공식 교육 시스템을 대체하지 않는다.
- 모든 PDF가 업로드만으로 완벽하게 자동 변환된다고 보장하지 않는다.
- 원본 시험지·교과서·원서 파일을 학생에게 공개 다운로드로 제공하지 않는다.
- `15분 학습` 메뉴나 고정 시간제 학습은 만들지 않는다.

## 5. 사용자 역할

### 학생

- 학생 계정으로 로그인한다.
- 중고등 내신과 원서리딩을 학습한다.
- 자신의 진행 상태, 점수, 오답, 보상을 확인한다.
- 관리자 자료와 다른 학생 기록에는 접근할 수 없다.

### 관리자/교사

- 학교, 문법 항목, 책, 학습 세트를 등록·수정·보관한다.
- PDF, Word, Excel, CSV 등의 자료를 업로드한다.
- 추출·생성된 문제를 검수하고 배포한다.
- 학생별 학습 현황과 오답 경향을 확인한다.
- 공개 콘텐츠 수정 시 기존 기록을 훼손하지 않고 새 버전을 만든다.

## 6. 정보 구조

### 학생용 최상위 메뉴

1. **중고등 내신**
2. **원서리딩**

각 메뉴에는 `S`, `B`, `文`, `Aa` 같은 문자 아이콘 대신 학습 성격을 나타내는 귀엽고 통일된 이미지 또는 SVG 아이콘을 사용한다.

### 전체 학습 구조

```text
중고등 내신
├─ 학교별 내신
│  └─ 학교 선택
│     ├─ 기출 모의고사
│     └─ 학교별 어휘학습
└─ 문법학습
   ├─ 관계사
   ├─ 분사
   ├─ to부정사
   └─ 관리자가 추가한 문법 항목

원서리딩
├─ Gangsta Granny
│  └─ Chapter 또는 범위별 Reading Quiz
├─ Wonder
└─ 관리자가 추가한 책
```

- 홈에서는 특정 학교 이름을 노출하지 않는다.
- 학생 홈에는 중고등 내신과 원서리딩 두 개의 학습 메뉴만 표시한다.
- 중고등 내신에 들어가면 학교별 내신과 문법학습으로 나눈다.
- 학교별 내신에 들어간 후 세화여중, 선화여중, 대원외고 등 관리자가 등록한 학교를 보여준다.
- 학교 선택 후 기출 모의고사와 학교별 어휘학습 두 메뉴를 표시한다.
- 학교 목록과 순서는 관리자가 변경한다.
- 문법학습은 특정 학교 하위가 아니라 중고등 내신의 공통 학습 메뉴다.
- 원서리딩은 중고등 내신과 분리된 독립 최상위 메뉴다.

## 7. 학생용 기능

### 7.1 학생 홈

- ENGLISH ARCHIVE 로고와 서비스명
- 중고등 내신과 원서리딩의 큰 메뉴 카드 2개
- 각 카드의 이미지형 아이콘, 짧은 설명, 진행 상태 요약
- 최근 학습 기록
- 학습 냉장고 보상 현황
- 모바일에 적합한 일관된 내비게이션

### 7.2 진행 상태

| 상태 | 조건 |
|---|---|
| 학습 전 | 제출 기록 없음 |
| 학습 중 | 문제를 시작했지만 제출하지 않았거나 일부 세트만 완료 |
| 학습 완료 | 해당 세트를 제출하고 결과가 저장됨 |
| 재학습 필요 | 오답이 있고 재학습을 완료하지 않음 |
| 재학습 완료 | 해당 시도의 오답을 다시 풀어 정답 처리함 |

- 상태는 브라우저가 아니라 학생 계정에 저장한다.
- 콘텐츠 제목 수정이나 새 버전 배포 후에도 과거 완료 기록을 유지한다.

### 7.3 학교별 어휘

- 단어 학습은 **한 세트당 25~50문제**로 구성한다.
- 관리자가 자료마다 세트 크기를 25~50문제 범위에서 선택한다.
- 예를 들어 100개 단어를 25문제씩 구성하면 `1–25`, `26–50`, `51–75`, `76–100`으로 표시한다.
- 마지막 세트는 선택한 세트 크기보다 적을 수 있다.
- 객관식 빈칸 채우기를 기본 문제 유형으로 한다.
- 보기 순서는 섞을 수 있지만 정답과 문장 의미는 변경하지 않는다.
- 세트별 점수, 완료 여부, 최근 시도일을 표시한다.
- 오답만 다시 풀 수 있다.

### 7.4 문법학습 자료

- 학교 배부 문법 프린트나 교사가 등록한 문법 자료를 기반으로 한다.
- 관계사, 분사, to부정사 등 문법 아이템별로 관리한다.
- 문제 세트에 출처 범위, 문제 수, 상태를 표시한다.
- 생성 문제는 관리자 검수 후에만 공개한다.

### 7.5 기출유형 모의고사

관리자는 한 시험 세트에 다음 자료를 연결한다.

1. **지난 기출 시험지 1개 이상**: 여러 회차·학기의 시험지를 복수 업로드할 수 있다.
2. **기준 기출 시험지 1개**: 업로드한 기출 중 새 모의고사의 정확한 문항 수·순서·객관식·서술형 구성을 결정한다.
3. **보조 기출 시험지 0개 이상**: 반복 출제 유형, 표현 방식, 난이도와 학교별 출제 경향 분석에 함께 사용한다.
4. **이번 시험범위 지문 텍스트**: 새 문제의 내용과 출제범위다.

기준 기출 시험지와 이번 시험범위 지문 텍스트가 모두 등록되어야 모의고사를 생성할 수 있다. 하나라도 없으면 생성 버튼을 비활성화하고 부족한 자료를 안내한다.

- 기출 시험지는 한 번에 여러 파일을 선택하거나 여러 차례 나누어 추가할 수 있다.
- 각 기출에 시험명, 학년도, 학기, 중간·기말 구분을 입력할 수 있다.
- 관리자는 업로드한 기출 중 언제든 기준 기출을 변경할 수 있다.
- 시험지별 문항 구성이 서로 다르면 시스템이 차이를 비교하여 보여준다.
- 기준 기출을 변경하면 기존 생성본을 덮어쓰지 않고 새로운 모의고사 버전을 생성한다.

생성 규칙:

- 새 문제의 내용과 근거는 **이번 시험범위 지문 텍스트에서만** 출제한다.
- 모든 기출 시험지는 문제의 유형, 표현 방식, 학교별 출제 경향 및 난이도 분석에 참고한다.
- 정확한 전체 문제 수, 객관식·서술형 수, 문항 순서와 답안 형식은 **관리자가 선택한 기준 기출 시험지**를 따른다.
- 전체 문제 수는 기준 기출 시험지와 정확히 같아야 한다.
- 객관식 문제 수와 서술형 문제 수를 각각 따로 분석하고 기준 기출과 정확히 동일하게 맞춘다.
- 예: 기준 기출이 객관식 20문제와 서술형 5문제라면 새 모의고사도 객관식 20문제와 서술형 5문제로 생성한다.
- 가능하면 기준 기출의 문항별 유형을 순서대로 대응한다. 예를 들어 1번 어휘, 2번 내용 일치, 3번 문법, 21번 영작이라면 새 시험도 같은 위치에 같은 유형을 배치한다.
- 객관식은 기준 기출과 같은 보기 수를 기본으로 하며 정답이 보기에 정확히 포함되어야 한다.
- 서술형은 기준 기출의 단답형, 문장 완성, 영작, 조건형 서술 등 답안 형식을 구분하여 대응한다.
- 보조 기출에서 반복되는 유형은 출제 경향 정보로 활용하되 기준 기출의 정확한 문항 구성을 변경하지 않는다.
- 서술형 문항에는 모범답안, 허용 가능한 대체답안, 채점 기준을 저장한다.
- 기출 문항을 그대로 복제하지 않는다.
- 문제 수, 객관식·서술형 구분 또는 문항 유형을 인식하지 못하면 임의로 추정하여 생성하지 않는다. 관리자가 분석 결과를 확인·수정한 후에만 생성한다.
- AI 생성 직후에는 `검수 필요` 상태이며 학생에게 보이지 않는다.
- 관리자가 배포하면 해당 학교 학생 화면에 즉시 표시한다.
- 시험 시기마다 시험지와 범위 자료를 새 버전으로 등록할 수 있다.

생성 전 관리자 확인 화면:

- 업로드한 전체 기출 시험지 목록
- 선택된 기준 기출과 보조 기출 구분
- 기출 시험지별 전체·객관식·서술형 문제 수 비교
- 기출 전체 문제 수
- 객관식 문제 수
- 서술형 문제 수
- 객관식 보기 수
- 문항 번호별 유형과 답안 형식
- 이번 시험범위 지문 목록
- 분석 오류 또는 확인이 필요한 항목

배포 전 자동 검증:

- `생성 전체 문제 수 = 기준 기출 전체 문제 수`
- `생성 객관식 수 = 기준 기출 객관식 수`
- `생성 서술형 수 = 기준 기출 서술형 수`
- 모든 생성 문항이 이번 시험범위 지문과 연결되어 있음
- 모든 객관식 문항에 보기와 유일한 정답이 있음
- 모든 서술형 문항에 모범답안과 채점 기준이 있음

위 검증 중 하나라도 실패하면 배포할 수 없다.

### 7.6 Grammar

- 관계사, 분사, to부정사 등 아이템별로 구성한다.
- 관리자가 문법 항목을 계속 추가·수정·정렬할 수 있다.
- 각 퀴즈는 **15문제 고정**이다.
- 객관식, 빈칸, 문장 판단 등 설정한 유형으로 구성한다.
- 제출 후 점수, 정답, 해설을 확인한다.
- 항목별 최근 점수와 최고 점수를 표시한다.

#### Grammar 오답 재학습

- 제출한 시도의 오답만 다시 풀 수 있다.
- 최초 선택과 정답 여부를 확인할 수 있다.
- 모든 오답을 맞히면 `재학습 완료`로 표시한다.
- 새로운 시도의 오답과 이전 시도의 오답을 구분한다.
- 관리자는 학생별·문법 항목별 오답 경향을 확인한다.

### 7.7 원서리딩

- 관리자는 여러 책을 등록한다.
- 학생은 제목과 표지 이미지를 보고 책을 선택한다.
- 책에 들어가면 Chapter 또는 지정 범위별 퀴즈를 확인한다.
- 한 퀴즈는 **5~20문제**다.
- 문제, 보기, 정답, 선택적 해설을 저장한다.

#### 작성된 Reading Quiz 보존 규칙

- 관리자가 완성된 문제와 답을 올리면 문구를 바꾸지 않는다.
- 문제, 보기, 순서, 정답을 업로드 내용 그대로 저장·배포한다.
- 맞춤법 자동수정이나 AI 재작성도 기본 수행하지 않는다.
- 수정이 필요하면 관리자가 새 버전을 만든다.
- Gangsta Granny Reading Quiz 20문제에 이 원칙을 적용한다.

### 7.8 공통 퀴즈 플레이어

- 현재 문제 번호와 전체 문제 수
- 이전/다음 이동 및 전체 답안지
- 선택 답안 자동 저장
- 미응답 문항 표시와 제출 전 확인
- 중복 제출 방지
- 새로고침 또는 네트워크 중단 후 이어풀기
- 제출 후 점수, 문항별 정오답, 정답, 해설
- 모바일 터치 영역 최소 44px
- 키보드 접근성 지원

#### 콘텐츠별 정오답 공개 방식

| 콘텐츠 | 풀이 중 정오답 공개 | 최종 결과 공개 |
|---|---|---|
| 기출유형 모의고사 | 공개하지 않음 | 모든 문제를 다 풀고 최종 제출한 뒤 공개 |
| 학교별 어휘 | 문제별 답안 확정 직후 공개 | 전체 완료 후 점수와 오답 요약 |
| 문법학습 | 문제별 답안 확정 직후 공개 | 전체 완료 후 점수와 오답 요약 |
| Grammar | 문제별 답안 확정 직후 공개 | 전체 완료 후 점수와 오답 재학습 연결 |
| 원서리딩 Reading Quiz | 문제별 답안 확정 직후 공개 | 전체 완료 후 점수와 오답 요약 |

즉시 공개형 인터랙티브 문제의 동작:

- 학생이 답을 선택한 뒤 `정답 확인`을 누르면 해당 답안을 확정한다.
- 정답이면 정답 표시와 선택적 해설을 즉시 보여준다.
- 오답이면 오답 표시, 정답, 선택적 해설을 즉시 보여준다.
- 정답을 본 뒤 점수 조작을 막기 위해 확정한 답안은 변경할 수 없다.
- 학생은 피드백을 확인한 뒤 다음 문제로 이동한다.
- 오답 정보는 즉시 저장되어 전체 완료 후 오답 재학습에 사용한다.
- 관리자가 해설 공개 여부를 콘텐츠별로 설정할 수 있지만 정오답과 정답은 즉시 공개한다.

기출유형 모의고사의 동작:

- 풀이 중에는 정답, 오답 여부, 해설, 중간 점수를 표시하지 않는다.
- 학생은 최종 제출 전까지 답을 자유롭게 변경할 수 있다.
- 미응답 문제가 있으면 최종 제출 전에 안내한다.
- 최종 제출을 확인한 뒤 전체 점수, 객관식 정오답, 서술형 모범답안·채점 기준을 공개한다.
- 서술형 자동채점이 확실하지 않으면 `채점 대기`로 표시하고 교사 검수 후 최종 점수를 확정할 수 있다.
- 제출 후에는 답안을 변경할 수 없다.

### 7.9 학습 냉장고 보상

- 학생용 명칭: **Teacher's Treat Fridge**
- 보조 문구: **Study, earn, and pick your treat!**
- 완료한 학습 세트가 쌓이면 보상 선택권을 얻는다.
- 특정 간식을 자동 지급하지 않는다.
- `다음 보상: 망고`처럼 다음 간식을 미리 지정하지 않는다.
- 학생이 20개 중 원하는 간식을 직접 선택한다.
- 선택 전 확인 단계와 선택 이력을 제공한다.
- 실제 지급 확인은 관리자가 처리할 수 있게 확장한다.

기본 간식 20종:

1. 떡볶이
2. 망고
3. 수박
4. 아이스크림
5. 초콜릿
6. 콜라
7. 주스
8. 소시지
9. 젤리
10. 쿠키
11. 햄버거
12. 피자
13. 감자튀김
14. 치킨
15. 도넛
16. 컵케이크
17. 딸기 케이크
18. 버블티
19. 팝콘
20. 프레첼

간식은 귀여운 이미지로 표시하며 관리자가 종류, 이미지, 필요 완료 수와 활성 상태를 변경할 수 있다.

## 8. 관리자 기능

### 8.1 인증과 대시보드

- 로그인하지 않은 사용자는 관리자 페이지와 API를 사용할 수 없다.
- 운영 비밀번호와 API 키를 코드에 저장하지 않는다.
- Supabase Auth와 역할 기반 권한을 사용한다.
- 대시보드에 전체 학생, 배포 콘텐츠, 검수 필요 콘텐츠, 최근 업로드·학습, 처리 실패를 표시한다.

### 8.2 학교·항목·책 관리

- 학교 추가, 수정, 비활성화, 정렬
- 문법 항목 추가, 수정, 비활성화, 정렬
- 원서리딩 책과 표지, Chapter/범위 추가
- 삭제보다 보관을 기본으로 하여 기존 학습 기록을 유지한다.

### 8.3 자료 업로드

지원 형식:

- PDF
- DOCX
- XLSX
- CSV/TSV
- TXT/JSON
- 이미지 — OCR은 확장 단계

공통 입력값:

- 자료 제목과 종류
- 연결할 학교, 문법 항목 또는 책
- 시험/단어/Chapter 범위
- 버전명과 설명
- 파일

처리 상태:

1. 업로드 중
2. 텍스트 추출 중
3. 분석 완료
4. 문제 인식 완료 또는 생성 대기
5. 검수 필요
6. 배포 중
7. 보관
8. 처리 실패

보존 원칙:

- 수정 시 기존 파일과 문제를 덮어쓰지 않고 새 버전을 만든다.
- 이전 버전에 연결된 학생 기록은 유지한다.
- 원본 파일, 추출 텍스트, 생성 문제, 수정 이력을 구분한다.
- 명시적인 영구 삭제 승인 전에는 원본을 삭제하지 않는다.

### 8.4 문제 편집·검수·배포

- 문제 추가, 수정, 복제, 순서 변경, 비활성화
- 보기 수정과 정답 지정
- 해설 입력과 학생 화면 미리보기
- 필수 항목, 중복 보기, 정답 불일치 검사
- `초안 → 검수 필요 → 배포 중 → 비공개/보관` 상태 관리
- 전체 승인 후 배포
- 비공개 후에도 과거 학생 기록 유지

### 8.5 학생 학습 현황

- 학생별 최근 학습일과 완료 세트 수
- 세트별 점수와 시도 횟수
- Grammar 오답과 재학습 상태
- 간식 선택권, 선택 간식, 지급 상태
- 학교·문법 항목·책 필터

## 9. 콘텐츠 생성 원칙

### 자동 생성 가능

- 학교 어휘 빈칸 객관식
- 문법학습 프린트 기반 문제
- 공용 Grammar 15문제
- 기출유형 모의고사

### 자동 생성 금지

- 관리자가 완성된 문제와 답을 제공한 원서리딩 Reading Quiz
- `원문 그대로 사용`으로 표시한 문제 세트

### AI 안전장치

- 구조화된 JSON 스키마로 응답받는다.
- 요청 문제 수와 생성 문제 수가 다르면 실패 처리한다.
- 각 문제에 질문, 보기, 정답, 해설을 포함한다.
- 정답이 보기에 없으면 검수 오류로 표시한다.
- 자동 배포하지 않는다.
- 교사 수정 내용을 AI 재생성으로 덮어쓰지 않는다.
- 생성 요청과 사용 출처 버전을 기록한다.
- 기출유형 모의고사는 전체 수뿐 아니라 객관식·서술형 수를 각각 검증한다.
- 기출유형 모의고사의 각 문항에 참고한 기출 문항 유형과 근거 시험범위 지문을 기록한다.

## 10. 핵심 사용자 흐름

### 학생 어휘 학습

1. 로그인한다.
2. 학교별 내신과 자신의 학교를 선택한다.
3. 학교별 어휘에서 원하는 번호 범위의 세트를 시작한다.
4. 관리자가 설정한 25~50문제를 풀고 제출한다.
5. 점수와 오답을 확인한다.
6. 학습 완료 또는 재학습 필요 상태가 저장된다.

### Grammar 오답 재학습

1. 문법 항목의 15문제를 제출한다.
2. 결과에서 오답 재학습을 누른다.
3. 틀린 문제만 다시 푼다.
4. 모두 맞히면 재학습 완료가 저장된다.

### 보상 선택

1. 학습 완료로 보상 선택권을 얻는다.
2. Teacher's Treat Fridge를 연다.
3. 간식 20개 중 하나를 선택한다.
4. 선택을 확인하면 획득 이력에 저장된다.

### 관리자 기출유형 모의고사

1. 학교와 시험명을 선택한다.
2. 지난 기출 시험지를 1개 이상 올리고 이번 시험범위 지문 텍스트를 업로드한다.
3. 시스템이 모든 기출의 전체·객관식·서술형 문제 수와 문항별 유형을 각각 분석한다.
4. 관리자가 정확한 문항 구성 기준이 될 기출 1개를 선택하고 나머지는 보조 기출로 둔다.
5. 관리자가 시험지별 분석 결과와 차이를 확인하고 필요하면 수정한다.
6. 시스템이 이번 시험범위 지문만 사용하여 기준 기출과 동일한 문항 구성의 새 문제를 생성한다.
7. 생성된 전체·객관식·서술형 문제 수가 기준 기출과 각각 일치하는지 자동 검증한다.
8. 관리자가 문제, 정답, 서술형 채점 기준을 검수한다.
9. 배포하면 학생 해당 학교 목록에 나타난다.

### 관리자 원서리딩 퀴즈

1. 책과 Chapter/범위를 선택한다.
2. 문제·답 파일을 업로드한다.
3. 시스템은 문구를 바꾸지 않고 문제, 보기, 정답을 분리한다.
4. 관리자가 원본 대조 후 배포한다.

## 11. 화면 목록

### 학생용

- `/` 홈
- `/login` 로그인
- `/exam-prep` 중고등 내신
- `/school` 학교 선택
- `/school/[schoolId]` 학교별 내신
- `/school/[schoolId]/vocabulary` 어휘
- `/school/[schoolId]/mock-exams` 기출유형 모의고사
- `/grammar` 문법 항목
- `/grammar/[topicId]` 항목 상세
- `/novel-study` 책 목록
- `/novel-study/[bookId]` 책 상세
- `/quiz/[setId]` 퀴즈 플레이어
- `/quiz/[setId]/result/[attemptId]` 결과
- `/review/grammar` 오답 재학습
- `/progress` 학습 이력
- `/treat-fridge` 간식 보상
- `/profile` 프로필

### 관리자용

- `/admin/login` 로그인
- `/admin` 대시보드
- `/admin/schools` 학교 관리
- `/admin/schools/[schoolId]` 학교 콘텐츠
- `/admin/grammar` Grammar 관리
- `/admin/books` 원서리딩 관리
- `/admin/uploads` 업로드 현황
- `/admin/content/[contentId]` 문제 편집·검수
- `/admin/students` 학생 관리
- `/admin/progress` 학습 현황
- `/admin/rewards` 보상 관리
- `/admin/settings` 설정

## 12. 데이터 모델

### profiles

- id: Supabase Auth 사용자 UUID
- role: student, teacher, admin
- display_name
- school_id nullable
- active
- created_at, updated_at

### schools / grammar_topics / books

- 공통: id, name/title, image_url, display_order, active, created_at, updated_at
- books 추가: author, description

### learning_contents

- id
- content_type: vocabulary, school_grammar, mock_exam, grammar, novel_quiz
- title
- school_id, grammar_topic_id, book_id nullable
- scope_label
- status: draft, processing, review, published, hidden, archived, failed
- question_count
- settings JSONB: 어휘 세트 크기 등 콘텐츠별 설정
- feedback_mode: immediate 또는 after_submission
- source_locked: 원문 변경 금지 여부
- display_order
- published_at
- created_by, created_at, updated_at

### content_versions

- id, content_id, version_number
- title_snapshot
- settings JSONB
- mock_exam_blueprint JSONB nullable: 기출 전체·객관식·서술형 수, 문항별 유형·보기 수·답안 형식
- status, created_by, created_at

### source_files

- id, content_version_id
- source_role: vocabulary, grammar_print, past_exam, textbook_range, reading_quiz
- exam_role nullable: primary 또는 reference
- exam_year, exam_term, exam_period nullable
- original_filename, storage_path, mime_type, file_size
- extracted_text, extraction_status, checksum, created_at

### questions

- id, content_version_id, position
- question_type, prompt, choices JSONB
- correct_answer JSONB, explanation
- source_reference nullable
- locked, active
- exam_section nullable: objective 또는 subjective
- source_blueprint JSONB nullable: 대응 기출 유형과 시험범위 근거
- accepted_answers JSONB nullable: 서술형 대체답안
- grading_rubric JSONB nullable: 서술형 채점 기준

### quiz_attempts / attempt_answers

- 시도: user_id, content_version_id, status, 시작·제출 시각, 점수, 정답 수, 전체 수
- 답안: attempt_id, question_id, selected_answer, is_correct, answered_at

### wrong_answer_reviews

- user_id, source_attempt_id, question_id
- status: pending, completed
- review_attempt_count, completed_at

### reward_items / reward_credits / reward_selections

- 간식: 이름, 이미지, 필요 완료 수, 순서, 활성 상태
- 선택권: 사용자, 발생한 학습 시도, available/used 상태
- 선택: 사용자, 선택권, 간식, selected/delivered 상태와 시각

## 13. 권한과 보안

Supabase Row Level Security를 필수 적용한다.

| 데이터 | 학생 | 관리자 |
|---|---|---|
| 배포 콘텐츠 | 조회 | 전체 관리 |
| 초안/검수 콘텐츠 | 접근 불가 | 접근 가능 |
| 원본 파일 | 접근 불가 | 권한 관리자만 |
| 본인 학습 기록 | 조회·생성 | 조회 가능 |
| 다른 학생 기록 | 접근 불가 | 권한 범위 조회 |
| 정답 | 제출 전 접근 불가 | 관리 가능 |
| 보상 | 본인 선택·조회 | 지급 처리 |

### 정답 보호

- 퀴즈 시작 응답에 정답을 포함하지 않는다.
- 채점은 서버에서 한다.
- 어휘·문법·원서리딩은 해당 문제 답안을 서버에 확정한 후 그 문제의 정답과 해설만 반환한다.
- 기출유형 모의고사는 전체 최종 제출 전까지 어떤 문제의 정답·정오답·해설도 반환하지 않는다.
- 학생에게 questions 테이블 전체 공개 권한을 주지 않는다.

### 파일 보호

- 시험지, 교과서 지문, 원서 퀴즈는 private Storage bucket에 저장한다.
- 관리자에게만 만료시간이 짧은 signed URL을 발급한다.
- 학생에게는 학습에 필요한 문제 데이터만 전달한다.

## 14. 기술 아키텍처

### 권장 스택

- Next.js App Router + TypeScript
- Tailwind CSS
- Vercel
- Supabase PostgreSQL, Auth, Storage
- Next.js Route Handlers / Server Actions
- OpenAI API
- Zod 입력 검증
- Sentry 또는 동급 오류 추적
- Vitest + Testing Library + Playwright

### Vercel 배포 원칙

- 현재 로컬 SQLite와 `data/uploads`를 운영 저장소로 사용하지 않는다.
- Vercel 로컬 파일 시스템에 영구 데이터를 저장하지 않는다.
- 영구 데이터는 Supabase PostgreSQL, 파일은 Supabase Storage에 저장한다.
- API 키와 비밀값은 Vercel Environment Variables에만 저장한다.
- Preview와 Production 환경을 분리한다.
- 오래 걸리는 PDF 분석·AI 생성은 확장 시 별도 queue/worker로 분리한다.

## 15. API 초안

### 학생

- `GET /api/student/home`
- `GET /api/schools`
- `GET /api/schools/[id]/contents`
- `GET /api/grammar/topics`
- `GET /api/books`
- `POST /api/quiz/[contentVersionId]/start`
- `PUT /api/attempts/[attemptId]/answers`
- `POST /api/attempts/[attemptId]/answers/[questionId]/check` — 즉시 공개형 문제의 답안 확정과 피드백
- `POST /api/attempts/[attemptId]/submit`
- `GET /api/attempts/[attemptId]/result`
- `POST /api/reviews/[attemptId]/start`
- `POST /api/rewards/select`

### 관리자

- `GET/POST/PATCH /api/admin/schools`
- `GET/POST/PATCH /api/admin/grammar-topics`
- `GET/POST/PATCH /api/admin/books`
- `POST /api/admin/uploads`
- `GET /api/admin/uploads/[id]/status`
- `POST /api/admin/content/[id]/generate`
- `PATCH /api/admin/questions/[id]`
- `POST /api/admin/content/[id]/publish`
- `POST /api/admin/content/[id]/hide`
- `GET /api/admin/progress`
- `POST /api/admin/rewards/[selectionId]/deliver`

모든 API는 로그인, 역할, 입력 스키마를 서버에서 검증한다.

## 16. 비기능 요구사항

### 모바일·성능

- 360px 스마트폰부터 정상 작동한다.
- 홈 주요 콘텐츠는 모바일 4G 기준 2.5초 이내 표시를 목표로 한다.
- 이미지 최적화, 지연 로딩, 목록 페이지네이션을 적용한다.
- 자동 저장은 짧은 지연 후 묶어서 전송한다.

### 접근성

- 색상만으로 정답·오답·상태를 구분하지 않는다.
- 입력과 버튼에 읽을 수 있는 이름을 제공한다.
- 키보드로 퀴즈를 풀 수 있다.
- WCAG 2.1 AA 수준의 대비를 목표로 한다.
- 간식 이미지에 대체 텍스트를 제공한다.

### 안정성

- 답안 저장 실패 시 알림과 재시도를 제공한다.
- 여러 탭에서 같은 시험 중복 제출을 방지한다.
- 배포 문제 버전은 학생 시도 중 변경하지 않는다.
- 데이터베이스 백업과 복구 절차를 마련한다.

### 개인정보·저작권

- 학생 정보는 최소한만 저장하고 학습 기록을 공개하지 않는다.
- 미성년자를 고려하여 불필요한 추적·광고를 넣지 않는다.
- 학교 시험지, 교과서, 원서 자료는 수업 권한 범위에서만 사용한다.
- 원본 파일은 공개 검색·다운로드 대상으로 제공하지 않는다.
- 기출유형 생성 시 원문 문제를 복제하지 않고 구조만 참고한다.

## 17. 디자인 방향

### 17.1 레퍼런스

- Wix `App Landing Page` 템플릿 2350의 밝고 경쾌한 교육 앱 분위기를 주요 레퍼런스로 삼는다.
- 참고 요소는 전체적인 분위기, 여백, 색상 사용법, 타이포그래피 대비, 교육용 일러스트 배치다.
- 원본 레이아웃, 문구, 로고, 캐릭터, 일러스트를 그대로 복제하지 않고 ENGLISH ARCHIVE만의 화면과 자산으로 새로 제작한다.

### 17.2 비주얼 콘셉트

- 밝고 따뜻한 미색 또는 흰색 배경을 기본으로 한다.
- 제목은 굵고 선명한 검정 계열의 큰 글자로 표현한다.
- 산호색, 보라색, 연두색, 노란색을 제한된 포인트 색상으로 사용한다.
- 연필, 책, 폴더, 별, 잎사귀, 간식 등 교육과 학습을 연상시키는 손그림풍의 평면 일러스트를 사용한다.
- 장식 요소는 화면 가장자리나 카드 주변에 비대칭으로 배치하여 활기를 주되 문제 풀이를 방해하지 않는다.
- 충분한 여백, 단순한 정보 구조, 굵은 제목과 짧은 설명의 대비를 유지한다.
- 과도한 그라데이션, 유리 효과, 복잡한 그림자, 문자 한 글자 아이콘은 사용하지 않는다.
- 친근하고 귀엽지만 유아용처럼 보이지 않는 중·고등학생 수준의 표현을 사용한다.

### 17.3 권장 색상 토큰

| 용도 | 권장 색상 |
|---|---|
| 기본 배경 | Warm White `#FFFDF7` |
| 기본 글자 | Ink Black `#17151A` |
| 브랜드 포인트 | Archive Purple `#7354AA` |
| 주요 강조 | Coral `#F47C6C` |
| 보조 강조 | Soft Lime `#C9E77D` |
| 주의·보상 | Sunny Yellow `#F7D96F` |
| 카드 경계 | Soft Gray `#E8E3EA` |

- 색상은 최종 UI 시안에서 접근성 대비를 확인한 후 조정한다.
- 한 화면에서 강한 포인트 색상을 지나치게 많이 동시에 사용하지 않는다.

### 17.4 타이포그래피

- 영문 제목은 둥글고 굵은 display sans-serif를 사용한다.
- 한글 제목은 굵고 가독성 높은 고딕 계열을 사용한다.
- 본문과 문제 문장은 장시간 읽어도 편한 고딕 계열을 사용한다.
- 큰 제목, 중간 제목, 본문, 보조 정보의 크기 차이를 명확하게 둔다.
- 장식용 손글씨체는 짧은 보상 문구에만 제한적으로 사용한다.

### 17.5 학생 홈 적용

- 상단에는 큰 환영 문구와 학습을 상징하는 전용 일러스트를 배치한다.
- 중고등 내신과 원서리딩은 여백이 넉넉한 2개의 큰 카드로 구성한다.
- 각 카드에는 문자 대신 서로 다른 교육 일러스트를 배치한다.
  - 학교별 내신: 시험지, 학교 건물, 연필
  - Grammar: 문장 블록, 퍼즐, 교정 연필
  - 원서리딩: 펼친 책, 책갈피, 이야기 캐릭터
- 카드는 제목, 한 줄 설명, 진행 상태, 시작 버튼 순으로 단순하게 구성한다.
- 최근 학습과 Teacher's Treat Fridge는 메인 메뉴보다 아래에 배치한다.

### 17.6 퀴즈 화면 적용

- 랜딩페이지의 장식성보다 문제 가독성과 답안 선택을 우선한다.
- 흰색 또는 미색 문제 카드에 굵은 문제 번호와 명확한 보기 버튼을 배치한다.
- 선택·정답·오답 상태는 색상, 테두리, 아이콘, 텍스트를 함께 사용한다.
- 즉시 피드백은 카드 아래에 부드럽게 펼쳐지는 영역으로 표시한다.
- 장식 일러스트는 결과 화면, 빈 상태, 보상 획득 화면에 집중적으로 사용한다.

### 17.7 관리자 화면 적용

- 학생용과 같은 색상·타이포그래피 체계를 사용하되 장식은 줄인다.
- 업로드, 검수, 배포 상태를 명확하게 확인할 수 있는 실용적인 카드와 표를 사용한다.
- 중고등 내신, 문법학습, 원서리딩 아이콘은 학생용과 동일한 일러스트 체계를 유지한다.
- 귀여운 분위기는 빈 상태, 완료 알림, 성공 메시지에 제한적으로 사용한다.

### 17.8 아이콘과 일러스트 제작 원칙

- 문자 한 글자를 아이콘으로 사용하지 않는다.
- 모든 일러스트는 동일한 선 굵기, 색상 팔레트, 모서리 스타일을 따른다.
- 작은 아이콘은 단순한 SVG, 큰 히어로 이미지는 최적화된 WebP 또는 SVG를 사용한다.
- 외부 템플릿 이미지를 복사하지 않고 상업적 사용이 가능한 자체 자산을 제작한다.
- 학생용과 관리자용 모두 같은 아이콘 원칙을 적용한다.

### 17.9 상태 표현

- 학습 전: 중립색 + 시작 전 텍스트
- 학습 중: 보라색 또는 파란색 + 진행률
- 학습 완료: 초록색 + 체크 아이콘
- 재학습 필요: 산호색 또는 주황색 + 오답 수
- 검수 필요: 관리자 화면에서 노란색
- 배포 중: 관리자 화면에서 초록색
- 상태는 항상 색상+텍스트+아이콘으로 표현한다.

## 18. 개발 우선순위

### P0 — 테스트 배포 필수

- Vercel용 Next.js 프로젝트
- Supabase 인증·DB·Storage
- 학생·관리자 로그인
- 학생용 최상위 메뉴 2개: 중고등 내신, 원서리딩
- 중고등 내신 하위 메뉴: 학교별 내신, 문법학습
- 학교별 내신 하위 메뉴: 기출 모의고사, 학교별 어휘학습
- 관리자 학교·문법 항목·책 관리
- 어휘 25~50문제 세트와 관리자 세트 크기 설정
- Grammar 15문제와 오답 재학습
- 원서리딩 5~20문제와 원문 보존
- 공통 퀴즈 플레이어와 서버 채점
- 학습 상태와 이력
- 관리자 업로드·검수·배포·버전 관리
- 기출 시험지+시험범위 연결 구조
- 간식 20종 선택과 보상 이력
- 모바일 반응형 UI

### P1

- AI 기출유형 모의고사 생성
- 문법학습 문제 생성
- 스캔 PDF OCR
- 학생별 통계
- 보상 지급 확인
- PWA 설치와 알림

### P2

- 반/그룹 관리와 과제 배정
- 학생별 맞춤 오답 세트
- 난이도 조절
- 학부모 요약
- 시험지 인쇄 PDF
- 고급 학습 분석

## 19. 개발 단계

1. 기존 학생·관리자 기능을 정리하고 Next.js UI로 이전한다.
2. Supabase 테이블, RLS, 인증, Storage를 구성한다.
3. 중고등 내신, 학교별 내신, 문법학습, 원서리딩과 퀴즈 플레이어를 구현한다.
4. 관리자 업로드, 문제 편집, 검수, 배포, 버전 이력을 구현한다.
5. Grammar 오답 재학습과 간식 보상을 구현한다.
6. Vercel Preview에 배포하고 학생 3~5명이 모바일 테스트한다.
7. 문제를 수정한 후 Production 배포와 도메인을 연결한다.
8. AI 생성과 OCR을 안정성·비용 측정 후 확장한다.

## 20. 테스트 계획

- 학교 추가 후 학생 학교 목록 반영
- 초안은 학생 비공개, 배포 후 즉시 공개
- 어휘가 관리자가 정한 25~50문제 단위로 정확히 분할되어 표시
- Grammar 퀴즈 정확히 15문제
- 원서리딩 퀴즈 5~20문제
- 원문 고정 퀴즈의 문구·보기·답 불변
- 기출 시험지를 2개 이상 업로드하고 기준 기출 1개를 선택 가능
- 기출 모의고사의 전체·객관식·서술형 문제 수가 기준 기출과 각각 정확히 일치
- 기출유형 모의고사의 모든 문제 내용이 이번 시험범위 지문에 근거
- 서술형 모든 문항에 모범답안과 채점 기준 존재
- 새로고침 후 답안 유지
- 제출 후 점수와 상태 저장
- 학교별 어휘·문법학습·원서리딩에서 문제별 답안 확정 직후 정오답과 정답 공개
- 즉시 공개형 문제에서 정답 확인 후 답안 변경 불가
- 기출유형 모의고사는 최종 제출 전 정답·정오답·점수 비공개
- 기출유형 모의고사는 최종 제출 전 답안 수정 가능, 제출 후 결과 공개
- Grammar 오답만 재학습
- 간식 20종 중 학생 직접 선택, 다음 보상 미지정
- 학생의 관리자 API 차단
- 제출 전 정답 비노출
- 다른 학생 기록과 원본 파일 접근 차단
- iPhone Safari, Android Chrome, 태블릿, Windows Chrome/Edge 테스트

## 21. 출시 승인 기준

- Vercel Production URL에서 학생과 관리자가 로그인한다.
- 업로드 자료가 재배포 후에도 사라지지 않는다.
- 공개 콘텐츠만 학생에게 표시된다.
- 학생 기록이 다른 기기에서도 유지된다.
- 모의고사는 최종 제출 후에만 결과를 공개하고, 나머지 학습 퀴즈는 문제별 즉시 피드백을 제공한다.
- 어휘 세트가 25~50문제 범위이고 Grammar가 15문제 규칙을 만족한다.
- 기출유형 모의고사는 지난 기출과 전체·객관식·서술형 문제 수가 각각 정확히 일치한다.
- 원서리딩 원문 문제는 업로드 내용과 일치한다.
- Grammar 오답 재학습을 완료할 수 있다.
- 간식 선택권으로 20종 중 하나를 직접 선택한다.
- 관리자·학생 권한이 서버와 DB에서 분리된다.
- 360px 화면에서 가로 스크롤 없이 사용할 수 있다.
- 개인정보처리방침과 자료 사용 안내가 준비된다.
- 실제 학생 3~5명의 테스트에서 치명적 오류가 없다.

## 22. 성공 지표

- 학생 로그인 성공률 95% 이상
- 시작한 학습 세트 제출률 80% 이상
- 답안 자동 저장 실패율 1% 미만
- 배포 후 콘텐츠 누락 0건
- 문제·정답 불일치 1% 미만
- 관리자 콘텐츠 등록부터 학생 노출까지 10분 이내
- 주간 학습 완료 세트 수
- Grammar 오답 재학습 완료율
- 재시도 후 점수 향상도
- 보상 선택 완료율

## 23. 주요 위험과 대응

| 위험 | 대응 |
|---|---|
| Vercel 재배포 후 파일 소실 | Supabase DB와 Storage 사용 |
| 수정 시 기존 자료 소실 | 불변 버전과 보관 정책 |
| AI 오답·환각 | 자동 배포 금지, 검수와 정답 검사 |
| PDF 문항 수 인식 실패 | 관리자 확인과 수동 입력 |
| 스캔 PDF 추출 실패 | OCR 필요 표시, P1 OCR 지원 |
| 정답 노출 | 서버 채점과 RLS |
| 저작권 자료 공개 | private Storage, 문제 데이터만 전달 |
| 학생 개인정보 노출 | 최소 수집과 RLS |
| 긴 작업의 Vercel 제한 | 외부 worker/queue로 분리 |
| AI 비용 증가 | 관리자만 실행, 길이 제한, 사용량 기록 |
| 모바일에서 긴 어휘 세트의 피로 | 25~50문제 범위 선택, 자동 저장, 번호 이동, 진행률, 이어풀기 |

## 24. 환경변수

```text
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
APP_URL=
```

- Service Role Key와 OpenAI API Key는 클라이언트에서 사용하지 않는다.
- `.env.local`은 Git에 올리지 않는다.
- Preview와 Production의 비밀값과 데이터 정책을 분리한다.

## 25. 초기 테스트 데이터

- 학교: 세화여중 1개 이상
- 학교 어휘: 25~50문제 범위의 세트 1개 이상
- 문법학습: 세트 1개 이상
- 기출유형 모의고사: 테스트 세트 1개
- Grammar: 관계사, 분사, to부정사 각 15문제
- 원서리딩: Gangsta Granny Reading Quiz 20문제
- 간식: 20종
- 학생 테스트 계정: 3~5개
- 관리자 테스트 계정: 1개

실제 시험지와 학생 개인정보는 테스트 데이터와 분리한다.

## 26. 추후 결정 사항

- 학생 계정 생성 방식: 교사 발급, 초대코드, 이메일 가입
- 고정 도메인
- 보상 선택권 지급에 필요한 완료 세트 수
- 실제 간식 지급 확인을 MVP에 포함할지
- 해설을 즉시 공개할지 재시도 후 공개할지
- AI 모델과 월 예산
- 스캔 PDF OCR 제공자
- 학교별 학생 접근 제한 방식

## 27. 최종 제품 원칙

1. **자료는 사라지지 않는다.** 수정은 덮어쓰기가 아니라 새 버전으로 남긴다.
2. **학생에게는 검수된 문제만 보인다.** 생성과 배포는 별도 단계다.
3. **교사가 작성한 문제는 존중한다.** 원문 고정 Reading Quiz는 바꾸지 않는다.
4. **학습 기록은 계정에 남는다.** 한 기기나 브라우저에만 의존하지 않는다.
5. **모바일에서 가장 편해야 한다.** 학생의 실제 사용 환경을 우선한다.
6. **정답과 원본 자료를 보호한다.** 서버 채점과 비공개 저장소를 사용한다.
7. **보상은 학생이 고른다.** 다음 간식을 미리 지정하지 않는다.
